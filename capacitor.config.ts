import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_gestor_tareas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
